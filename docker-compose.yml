version: "3.9"
   
services:
  # db:
  #   image: postgres
  #   volumes:
  #     - ./data/db:/var/lib/postgresql/data
  #   environment:
  #     - POSTGRES_DB=postgres
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres

  redis:
    image: "redis:alpine"

  #nginx-web:
  #  image: "nginx:1.19.0-alpine"
  #  depends_on:
  #    - web
  #  volumes:
  #    - ./conf/etc/nginx:/etc/nginx/conf.d
  #  ports:
  #    - "8080:80"

  web:
    build: .
    environment:
      - APP_MODE=api
    volumes:
      - ./app-dummy:/code
    ports:
      - "8080:8080"
    depends_on:
      - redis
  
  celery:
    build: .
    environment:
      - APP_MODE=celery
    volumes:
      - ./app-dummy:/code
    depends_on:
      - redis
  
  schedule:
    build: .
    environment:
      - APP_MODE=scheduler
    volumes:
      - ./app-dummy:/code
    depends_on:
      - redis