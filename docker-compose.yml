version: "3.9"
   
services:
  # db:
  #   image: postgres
  #   volumes:
  #     - ./data/db:/var/lib/postgresql/data
  #   environment:
  #     - POSTGRES_DB=postgres
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres

  redis:
    image: "redis:alpine"

  web:
    build: .
    environment:
      - APP_MODE=api
    volumes:
      - ./app-dummy:/code
    ports:
      - "8000:8000"
    #depends_on:
    #  - db